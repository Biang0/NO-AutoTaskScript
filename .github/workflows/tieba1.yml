name: tieba1

on:
  workflow_dispatch:
  schedule:
    - cron: "0 16 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # 这个 v4 已经支持 Node24，不用改

      - name: Use Node.js 14
        uses: actions/setup-node@v4  # v4 已支持，不用改
        with:
          node-version: '14.x'

      - name: Cache node_modules
        uses: actions/cache@v4  # 从 v3 → v4
        id: cache-node-modules
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ steps.setup-node.outputs.node-version }}-nodeModules-${{ hashFiles('package-lock.json') }}-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup-node.outputs.node-version }}-nodeModules-

      - name: Install dependency packages
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: |
          npm install

      - name: Start running the script
        run: |
          node src/client/sudojia_tieba_taskSign.js
        env:
          TIE_BA_COOKIE: ${{ secrets.TIE_BA_COOKIE }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_USER_ID: ${{ secrets.TG_USER_ID }}
          PUSH_KEY: ${{ secrets.PUSH_KEY }}
          BARK_PUSH: ${{ secrets.BARK_PUSH }}
          BARK_SOUND: ${{ secrets.BARK_SOUND }}
          DD_BOT_TOKEN: ${{ secrets.DD_BOT_TOKEN }}
          DD_BOT_SECRET: ${{ secrets.DD_BOT_SECRET }}
          QYWX_KEY: ${{ secrets.QYWX_KEY }}
          IGOT_PUSH_KEY: ${{ secrets.IGOT_PUSH_KEY }}
          PUSH_PLUS_TOKEN: ${{ secrets.PUSH_PLUS_TOKEN }}
          PUSH_PLUS_USER: ${{ secrets.PUSH_PLUS_USER }}

      - name: Delete Workflow Runs
        uses: Mattraks/delete-workflow-runs@v2  # 从 main → v2
        with:
          retain_days: 1
          keep_minimum_runs: 1
